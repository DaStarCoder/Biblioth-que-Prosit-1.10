#include <stdio.h>
#include <stdlib.h>

float a, b;
Comparaison(a,b){
	if (a<b){
		return a<b;
		printf("Le nombre %f", a "est plus petit que %f", b);
	}else if(a>b){
		return a>b;
		printf("Le nombre %f", a "est plus grand que %f", b);
	}else if(a == b){
		return a = b;
		printf("Le nombre %f", a "est égal au nombre %f", b);
	}else if(a == -b || -a == b){
		return a == -b
		printf("Le nombre %f", a "est l'opposé du nombre %f", b);
	}
}

int main(){
	Comparaison(a,b);
return 0;
}
